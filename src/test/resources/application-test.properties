# Test profile configuration
# This profile is used for running tests with TestContainers locally
spring.data.mongodb.uri=mongodb://localhost:27017/educational_quality_test

# Disable external service calls in test mode by default
# Tests will use VCR mocks instead
